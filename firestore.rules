rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /kanji_progress/{userId}/sections/{sectionId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
